machine: 
  services:
    - docker
dependencies:
  override:
    - docker-compose build
test:
  override:
    - docker-compose up -d
    - sudo lxc-attach -n "$(docker inspect --format "{{.Id}}" web)" -- bash -c pwd